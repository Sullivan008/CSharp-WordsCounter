<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Az online karakter számláló egy olyan ingyenes online eszköz, amely lehetővé teszi a karakterek, szavak, mondatok, bekezdések, stb. megszámlálását
          ezen felül, visszaadja a legsűrvebben használt szavakat (TOP) illetve az összes szó előfordulását, a szósűrűséggel meghatározva" />
    <meta name="keywords" content="karakter, betűk, mondatok, számol, online, counter, words, sentences, density" />
    <meta name="author" content="Alpha-Tech Software & Design Studio" />
    <title>@ViewBag.Title</title>

    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/style.css" rel="stylesheet" />

    <script src="~/Scripts/jquery-3.2.1.min.js"></script>

    @{
        SelectList GetLanguageDictionaryElement()
        {
            int previouslySelectedLanguageIndex = 0;
            HttpCookie languageCookie = HttpContext.Current.Request.Cookies["Language"];

            HashSet<SelectListItem> list = new WordsCounter.Controllers.Classes.Dictionary.LanguageDictionary().GetLanguageSelectListItem();

            if (languageCookie?.Value != null)
            {
                foreach (SelectListItem item in list)
                {
                    if (item.Text == languageCookie.Value)
                    {
                        previouslySelectedLanguageIndex = Int32.Parse(item.Value);
                        break;
                    }
                }
            }

            return new SelectList(list, "Value", "Text", previouslySelectedLanguageIndex);
        }
    }
</head>

<body style="padding-top:0px;">
    <div id="banner-area" class="banner-area">
        <div class="parallax-overlay"></div>
        <div class="banner-title-content">
            <div class="text-center">
                <h2>@WordsCounter.Resources.Shared.SharedTexts.Title</h2>
            </div>
        </div>
        <ul class="nav navbar-nav navbar-left top-box">
            <li scale="0" style="margin-top: 15px; margin-right: 10px;">@WordsCounter.Resources.Shared.SharedTexts.Language</li>
            <li scale="0" style="margin-top: 7px; margin-bottom: -10px">@Html.DropDownList("Languages", GetLanguageDictionaryElement(), new { @class = "form-control" })</li>
        </ul>
    </div>

    <div class="container body-content">
        @RenderBody()

        <hr />
        <footer>
            <center>
                <p>&copy; <strong>@DateTime.Now.Year - <a href="#">Alpha-Tech Software & Design Studio</a>.</strong></p>
            </center>
        </footer>
    </div>

    <script src="~/Scripts/SharedScripts/MyLayoutScript.js"></script>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")

    @RenderSection("scripts", required: false)
</body>
</html>
